# TypeScript SDK

A TypeScript SDK package managed in the repository's pnpm workspace and scaffolded with Vite+.

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp test
```

- Build the library:

```bash
vp pack
```
