{
  "name": "sandbox-toolkit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "turbo run check lint test build && vp check",
    "build": "turbo run build",
    "test": "turbo run test",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "turbo": "^2.10.13",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.4.2",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
